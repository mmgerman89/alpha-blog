<h1 align="center"><%= "Categoría: " + @category.name %></h1>

<div class="center">
  <% if logged_in? && current_user.admin? %>
    <div class="row">
      <span class="badge"><%= link_to 'Editar', edit_category_path(@category) %></span>
    </div>
  <% end %>
  <%= will_paginate @category_articles %>
</div>

<%= render 'articles/articles', obj: @category_articles %>

<div class="center">
  <%= will_paginate @category_articles %>
</div>